const eventRequestService = require('./eventRequest.service');
const systemSettings = require('./systemSettings.service');
const bloodBagRequestService = require('./bloodBagRequest.service');

module.exports = {
  eventRequestService,
  systemSettings
  ,bloodBagRequestService
};

