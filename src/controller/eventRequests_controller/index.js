/**
 * Event Requests Controller
 * 
 * Central export for event request controllers
 */

const eventRequestController = require('./eventRequest.controller');

module.exports = {
  eventRequestController
};

